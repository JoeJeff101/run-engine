# Diagrams

Mermaid sources. GitHub renders `.mmd` files and the fenced `mermaid` blocks in
the docs directly, so these exist for reuse and editing rather than because
anything needs them to display.

| File | Shows |
|---|---|
| `system-overview.mmd` | Thesis in, staged evidence out, including the reformulation loop |
| `board-phases.mmd` | The four phases and where work can stop early |
| `topology-sequential.mmd` | Context (build on) vs challenges (attack) across the board |
| `topology-tournament.mmd` | Temperature-diverged champions, scored judge, code-computed ranking |
| `topology-sole-writer.mmd` | Thread-per-role with a single writer |
| `evidence-gate.mmd` | The stage/promote boundary |
